📈 台股量化選股助手 - 系統手冊
本系統結合了 Streamlit 網頁展示與 GitHub Actions + Discord 自動化推播，旨在打造一個低基期、高殖利率的自動化選股環境。

🛠️ 系統三大功能
即時網頁掃描：透過 Streamlit 介面，手動調整參數並即時查看全市場篩選結果。

Discord 每日推播：每週一至週五 18:00 (台灣時間) 自動將精選標的送至 Discord 頻道。

雲端自動化運作：完全運行於 GitHub 雲端，不需佔用個人電腦資源。

⚙️ 如何修改選股參數？
如果你想調整選股的門檻（例如：想找更便宜的股票，或要求更高的殖利率），請依下列步驟修改：

1. 修改網頁版參數
打開 選股app.py。

找到側邊欄設定區（st.sidebar），修改 value 的數值。

存檔並推送到 GitHub，Streamlit 網頁會自動更新。

2. 修改 Discord 推播參數 (最常用)
打開 discord_bot.py。

找到「設定區」，修改以下變數：

PRICE_MIN: 最低股價（預設 10.0）

PRICE_MAX: 最高股價（預設 30.0）

MIN_YIELD: 最低殖利率要求（預設 5.0）

存檔並推送到 GitHub，下次自動推播就會套用新參數。

🔍 如何檢查系統是否正常執行？
如果某天沒收到 Discord 通知，可以透過以下方式除錯：

進入 GitHub 專案頁面，點擊 「Actions」 標籤。

點擊最近一次的任務（帶有紅色 ❌ 或綠色 ✅）。

點擊 「run-scanner」 查看詳細日誌。

✅ Discord 訊息發送成功：代表一切正常。

⚠️ 證交所 API 連線失敗：通常是假日或證交所伺服器維護中，隔天再試即可。

🛡️ 安全維護提醒
Webhook 網址：請永遠保持在 GitHub 的 Secrets 中，切勿直接寫死在程式碼並公開，以免頻道被騷擾。

SSL 豁免：程式碼中已加入 verify=False，這是為了確保能順利抓取政府公開資料，屬正常設定。
